name: CI

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Prepare verified generated snapshot
        id: generated_snapshot
        shell: bash
        env:
          GIT_CONFIG_GLOBAL: /dev/null
          GIT_CONFIG_NOSYSTEM: "1"
          GIT_LFS_SKIP_SMUDGE: "1"
          GIT_NO_LAZY_FETCH: "1"
          GIT_TERMINAL_PROMPT: "0"
        run: |
          set -euo pipefail
          snapshot_root="$(mktemp -d "${RUNNER_TEMP}/generated-mirror-snapshot.XXXXXX")"
          chmod 0700 "$snapshot_root"
          snapshot_root="$(cd "$snapshot_root" && pwd -P)"
          git \
            -c core.hooksPath=/dev/null \
            -c core.fsmonitor=false \
            -c submodule.recurse=false \
            clone --no-local --no-hardlinks --no-checkout \
            "$GITHUB_WORKSPACE" "$snapshot_root"
          git -C "$snapshot_root" \
            -c advice.detachedHead=false \
            -c core.hooksPath=/dev/null \
            -c core.fsmonitor=false \
            checkout --detach "$GITHUB_SHA"
          test "$(git -C "$snapshot_root" rev-parse --verify HEAD)" = "$GITHUB_SHA"
          python3 "$snapshot_root/scripts/verify_generated_sync_source_lock.py" \
            --repo-root "$snapshot_root" \
            --snapshot-root "$snapshot_root"
          printf 'path=%s\n' "$snapshot_root" >> "$GITHUB_OUTPUT"
      - name: Compile
        working-directory: ${{ steps.generated_snapshot.outputs.path }}
        run: python3 -m compileall -q scripts tests
      - name: Run tests
        working-directory: ${{ steps.generated_snapshot.outputs.path }}
        # Discovery keeps generated retention and scheduler/doctor suites in
        # the consumer gate without a second hand-maintained module list.
        run: python3 -m unittest discover -s tests
      - name: Validate sync manifest changes
        working-directory: ${{ steps.generated_snapshot.outputs.path }}
        env:
          BASE_REF: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: python3 scripts/validate_sync_manifest_changes.py --base-ref "$BASE_REF"

  python-39-compatibility:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.9"
      - name: Prepare verified generated snapshot
        id: generated_snapshot
        shell: bash
        env:
          GIT_CONFIG_GLOBAL: /dev/null
          GIT_CONFIG_NOSYSTEM: "1"
          GIT_LFS_SKIP_SMUDGE: "1"
          GIT_NO_LAZY_FETCH: "1"
          GIT_TERMINAL_PROMPT: "0"
        run: |
          set -euo pipefail
          snapshot_root="$(mktemp -d "${RUNNER_TEMP}/generated-mirror-snapshot.XXXXXX")"
          chmod 0700 "$snapshot_root"
          snapshot_root="$(cd "$snapshot_root" && pwd -P)"
          git \
            -c core.hooksPath=/dev/null \
            -c core.fsmonitor=false \
            -c submodule.recurse=false \
            clone --no-local --no-hardlinks --no-checkout \
            "$GITHUB_WORKSPACE" "$snapshot_root"
          git -C "$snapshot_root" \
            -c advice.detachedHead=false \
            -c core.hooksPath=/dev/null \
            -c core.fsmonitor=false \
            checkout --detach "$GITHUB_SHA"
          test "$(git -C "$snapshot_root" rev-parse --verify HEAD)" = "$GITHUB_SHA"
          python3 "$snapshot_root/scripts/verify_generated_sync_source_lock.py" \
            --repo-root "$snapshot_root" \
            --snapshot-root "$snapshot_root"
          printf 'path=%s\n' "$snapshot_root" >> "$GITHUB_OUTPUT"
      - name: Compile supported runtime
        working-directory: ${{ steps.generated_snapshot.outputs.path }}
        run: |
          python3 -m py_compile \
            scripts/codex_personal_sync.py \
            scripts/build_personal_codex_package.py \
            scripts/validate_sync_manifest_changes.py \
            scripts/verify_generated_sync_source_lock.py
      - name: Run Python 3.9 compatibility regressions
        working-directory: ${{ steps.generated_snapshot.outputs.path }}
        run: |
          python3 -m unittest \
            tests.test_personal_sync_reconciliation_safety.PendingLinkTransactionSafetyTests.test_empty_record_empty_state_transaction_parses_and_recovers \
            tests.test_personal_sync_reconciliation_safety.PendingLinkTransactionSafetyTests.test_oversized_json_integer_is_normalized_to_sync_error \
            tests.test_codex_personal_sync.CodexPersonalSyncTests.test_load_manifest_translates_oversized_json_integer \
            tests.test_package_builder_safety.PackageBuilderSafetyTests.test_manifest_parser_reports_deep_json_as_package_error \
            tests.test_package_builder_safety.PackageBuilderSafetyTests.test_manifest_parser_reports_oversized_integer_as_package_error \
            tests.test_release_manifest_baseline.ReleaseManifestBaselineTests.test_release_baseline_accepts_verified_release_history \
            tests.test_sync_manifest_changes.SyncManifestChangeTests.test_manifest_transition_counts_same_target_change_once \
            tests.test_sync_manifest_changes.ManifestSerializationSafetyTests.test_deep_raw_json_is_reported_as_validation_error \
            tests.test_sync_manifest_changes.ManifestSerializationSafetyTests.test_oversized_integer_is_reported_as_validation_error \
            tests.test_generated_sync_source_lock

  platform-safety:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Prepare verified generated snapshot
        id: generated_snapshot
        shell: bash
        env:
          GIT_CONFIG_GLOBAL: /dev/null
          GIT_CONFIG_NOSYSTEM: "1"
          GIT_LFS_SKIP_SMUDGE: "1"
          GIT_NO_LAZY_FETCH: "1"
          GIT_TERMINAL_PROMPT: "0"
        run: |
          set -euo pipefail
          snapshot_root="$(mktemp -d "${RUNNER_TEMP}/generated-mirror-snapshot.XXXXXX")"
          chmod 0700 "$snapshot_root"
          snapshot_root="$(cd "$snapshot_root" && pwd -P)"
          git \
            -c core.hooksPath=/dev/null \
            -c core.fsmonitor=false \
            -c submodule.recurse=false \
            clone --no-local --no-hardlinks --no-checkout \
            "$GITHUB_WORKSPACE" "$snapshot_root"
          git -C "$snapshot_root" \
            -c advice.detachedHead=false \
            -c core.hooksPath=/dev/null \
            -c core.fsmonitor=false \
            checkout --detach "$GITHUB_SHA"
          test "$(git -C "$snapshot_root" rev-parse --verify HEAD)" = "$GITHUB_SHA"
          python3 "$snapshot_root/scripts/verify_generated_sync_source_lock.py" \
            --repo-root "$snapshot_root" \
            --snapshot-root "$snapshot_root"
          printf 'path=%s\n' "$snapshot_root" >> "$GITHUB_OUTPUT"
      - name: Run platform personal sync safety tests
        working-directory: ${{ steps.generated_snapshot.outputs.path }}
        shell: bash
        run: |
          modules=(tests.test_personal_sync_reconciliation_safety)
          if [[ -f tests/test_release_retention.py ]]; then
            modules+=(tests.test_release_retention)
          fi
          if [[ -f tests/test_scheduler_doctor.py ]]; then
            modules+=(tests.test_scheduler_doctor)
          fi
          python3 -m unittest "${modules[@]}"
